<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Chat App</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 h-screen flex items-center justify-center">

  <div class="w-full max-w-md bg-white shadow-md rounded-lg flex flex-col h-[80vh]">

    <!-- Header -->
    <div class="bg-blue-600 text-white p-4 rounded-t-lg text-lg font-semibold">
      Chat App
    </div>

    <!-- Messages -->
    <div id="chat-container" class="flex-1 p-4 overflow-y-auto space-y-2">
      <!-- Messages will be dynamically added here -->
    </div>

    <!-- Input -->
    <!-- <form id="chat-form" class="flex items-center p-3 border-t">
      <input id="chat_input" name="message" 
        class="flex-1 border rounded-lg p-2 mr-2 focus:outline-none focus:ring"
        placeholder="Type a message..." />
        <input type="file" id="imageInput" accept="image/*" class="" />
      <input type="submit" value="Send"
        class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700" />
    </form> -->


    <form id="chat-form" class="flex items-center p-3 border-t space-x-2">
      <input id="chat_input" name="message"
        class="flex-1 border rounded-lg p-2 focus:outline-none focus:ring"
        placeholder="Type a message..." />
    
      <!-- File Input -->
      <input type="file" id="imageInput" accept="image/*" class="hidden" />
    
      <!-- Custom Label for File Input -->
      <label for="imageInput"
        class="cursor-pointer bg-gray-200 text-gray-700 px-3 py-2 rounded-lg hover:bg-gray-300">
        📷
      </label>
    
      <!-- Image Preview -->
      <img id="imagePreview" src="" class="w-10 h-10 object-cover rounded-full hidden border" />
    
      <input type="submit" value="Send"
        class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700" />
    </form>
    

  </div>

  <script src="/socket.io/socket.io.js"></script>
  <script src="/script/client.js"></script>
</body>
</html>
